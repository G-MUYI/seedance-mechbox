# Seedance Skills 规则

本文件是本仓库中所有 skill 的权威规则来源。如果本文件与任何 `skills/*/SKILL.md`、`skills/*/references/*` 或 UI 元数据文件冲突，请优先遵循本文件。

## 仓库结构

- 将可安装的 skill 放在 `skills/<skill-name>/` 中
- 保持一个顶级生成器模板或斜杠命令作为一个可安装的 skill。不要将命令级模板折叠成隐藏变体
- 保持每个 `SKILL.md` 简洁。它是入口点、触发面和导航指南，而不是完整的提示词库
- 将详细的生成规则、映射、示例和扩展提示词放在 `skills/<skill-name>/references/` 中
- 将跨 skill 研究和共享提示词编写知识放在 `skills/_shared/` 中
- 将 `agents/openai.yaml` 仅视为 UI 元数据。它不得重新定义 skill 行为

## 共享模块索引

`skills/_shared/` 下的模块文件各自负责一个关注点，本文件通过引用调度：

| 文件 | 职责 | 加载时机 |
|------|------|---------|
| `camera-audio-rules.md` | 镜头连续性、手持真实感、声音与文本限制、每拍镜头语言 | 每次生成提示词前必读（绝对优先级第一条） |
| `prompt-weight-rules.md` | 权重分配、压缩优先级、时间轴格式、默认输出策略 | 每次生成提示词前必读 |
| `reverse-engineering-template.md` | 倒推式分析的完整流程、五种形态类型表、盒体设计模板、示例对比 | 每次生成提示词前必读（核心方法论） |
| `completion-rules.md` | 阶段分配、复杂度控制、终态验收、常见失败修复 | 诊断或编辑结构盒体完成度、时长或结束状态一致性时加载 |
| `zodiac-beast-variant-base.md` | bust/crest/diorama/mask 四个变体的共享外壳特征表和输出段落 | 生成 zodiac beast 变体提示词时加载 |
| `seedance-prompt-research.md` | Seedance 2.0 提示词结构研究报告（归档参考） | 需要提示词质量诊断或跨 skill 优化时加载 |

## Skill 运行时工作流

### 核心原则：从目标对象反向工程

**不要套用固定模板填空。** 相反，分析目标对象的结构特征，反向设计可变形盒体。详细的倒推分析流程（形态类型识别、盒体设计、变形序列设计）见 `skills/_shared/reverse-engineering-template.md`。

1. 从用户的明确命令或请求的主题中选择 skill
2. 在应用任何详细生成规则之前，先阅读此 `AGENTS.md`
3. 仅读取所选 skill 的 `SKILL.md` 以了解触发范围和参考导航。它不得重新定义此文件中的行为
4. 在生成最终提示词之前，先确定**参考图模式**：
   - 如果用户附上图像或明确要求参考图版本，使用参考图版本并保留 `@Image` 角色行；`@Image` 必须同时作为最终目标外观、比例和材质来源。初始盒子必须先继承参考图材质，再嵌入精密结构：首帧可见外壳 60-70% 以上应呈现目标参考图的主材质、颜色比例、表面纹理、磨损、高光和软硬材料对比，功能分缝、铰链、螺栓、传动窗只是嵌入式功能结构，不能压过参考图材质
   - 如果用户明确要求无图版/纯文本版，使用无参考图版本，删除 `@Image` 角色行，并添加一条简短的 `最终目标：...` 外观描述行，同时说明可推断的关键材质
   - 如果用户未明确模式，在输出前先问一个简洁的问题：`是否使用参考图版？有图我会保留 @Image 角色行；无图我会删除 @Image 行并补最终目标外观特征。`
   - 对于批量输出，先问一次，然后将答案应用于整个批次，除非用户给出逐项模式
5. **首先分析目标对象**：按 `skills/_shared/reverse-engineering-template.md` 的四步流程执行（分析结构 → 倒推盒体 → 设计变形序列 → 生成提示词）
6. 读取所选 skill 的参考文件以获取领域特定的映射和输出形式：
   - 使用 `references/generator.md` 作为权威输出合同、映射来源、目标选择规则来源和回退行为来源
   - 不要使用或重新创建单独的短版/优化提示词模板
   - 如果任何参考文件与此 `AGENTS.md` 冲突，请遵循此文件。特别是，参考文件中的任何 `@Image` 要求仅适用于参考图模式
7. 仅当用户要求示例、完整提示词集或编号提示词输出时，才加载扩展提示词示例
8. 加载 `skills/_shared/camera-audio-rules.md` 和 `skills/_shared/prompt-weight-rules.md` 以应用镜头、音频、权重和输出策略规则
9. 当诊断或编辑结构盒体完成、时长或结束状态一致性时，加载 `skills/_shared/completion-rules.md`

## 输出规则

- 对于中文用户请求，默认使用中文解释
- 生成最终可用的 Seedance 提示词或内容输出，除非用户要求分析、规划或编辑指导
- 保留所选 skill 从其参考文件中获得的输出合同
- 最终可用的 Seedance 提示词不得出现直白的装置类别关键词；需要表达实体变形与内部传动时，统一改写为「可变形盒体」「封闭初始实体」「精密结构」「传动窗」「联动轮组」「物理展开」「结构接触声」等视觉化描述。skill 名、命令名和维护说明可以保留原有触发词，但不得带入最终提示词正文
- 最终可用的 Seedance 提示词不得暗示"完整零件或完整目标物先等比例缩小藏入盒体，再整体放大"的效果。所有体积变化必须来自可见折叠、套叠、分层收纳、滑轨推出、铰链翻开、伸缩节延展或壳体重新分配；禁止使用均匀缩放、无来源变大、完整小模型膨胀成大模型、整件复制增材等不符合物理逻辑的描述
- 为每个 skill 支持两种提示词模式：
  - **参考图版**：仅对用户实际提供或明确要求使用的资产包含清晰的 `@Image1`、`@Image2` 等角色分配行；角色分配必须写明参考图用于目标物外观、比例、关键部件和材质，并要求初始盒子的材质从参考图材质反推。最终提示词必须包含一条明确的"参考图材质锁定"句，说明初始盒体大面积外壳继承 @Image 的材质，精密结构只作为嵌入式分缝和功能件
  - **无参考图版**：删除所有未使用的 `@Image` 角色行，并用一条简洁的 `最终目标：...` 行替换失去的视觉指导，描述目标的关键轮廓、材质、正面/头部特征、支撑/肢体特征和标志性细节
- 当用户未明确模式时，不要在参考图版和无参考图版之间做出沉默选择。先问，再生成

### Skill 特定规则

- 默认遵循所选 skill 的完整输出合同。大多数 skill 输出详细提示词，但 `seedance-world-cup-mechbox` 定义了核心骨架完整提示词：保留其所需的镜头、主体、时间轴、结构、物理连续性和约束部分，无需冗余扩展。不要在所选 skill 的参考文件之外创建临时摘要
- 优先使用具体的视觉名词、物理变形节拍、镜头运动、材质细节、灯光和环境线索，而不是抽象形容词
- 保持一次性结构变形提示词从打开状态到最终揭示的连贯性
- 变形完成后的最后一步必须包含一个与目标物结构和主题相符的轻微小动作收尾，例如灯光轻闪、传感器微调、尾部轻摆、钳口轻合、排气阀短促泄压、支架轻微回弹；该动作只能发生在完整锁定之后，不能新增大件、不能改变主体姿态、不能暗示切镜
- 如果用户输入不完整，从所选 skill 的映射表中选择合理的默认值，并简要提及假设
- 不要在 `SKILL.md` 中复制冗长的参考内容

## 维护规则

- 新 skill 文件夹名称必须仅使用小写字母、数字和连字符
- 新 skill 前置元数据必须仅包含 `name` 和 `description`
- 长示例属于 `references/`，不属于 `SKILL.md`
- 共享规则首先放在这里。skill 特定的例外属于该 skill 的参考文件

## Skill 清单

本仓库目前包含十个可安装的 skill：

- `seedance-mechbox`
- `seedance-american-truckbox`
- `seedance-tiktok-us-mechbox`
- `seedance-country-mechbox`
- `seedance-zodiac-beast`
- `seedance-zodiac-beast-bust`
- `seedance-zodiac-beast-crest`
- `seedance-zodiac-beast-diorama`
- `seedance-zodiac-beast-mask`
- `seedance-world-cup-mechbox`
